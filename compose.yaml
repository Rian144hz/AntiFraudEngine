services:
  antifraud-engine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: antifraud-engine-app
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=test
    restart: always